// Destructuring de Arrays
const tecnologias = ['VueJS', 'JavaScript', 'NodeJS', 'HTML', 'CSS'];

const arrayForEach = tecnologias.forEach((tecnologia, index) => {
    return tecnologia;
})

const arrayMap = tecnologias.map((tecnologia, index) => {
    return tecnologia;
})

console.log(arrayForEach);
console.log(arrayMap);