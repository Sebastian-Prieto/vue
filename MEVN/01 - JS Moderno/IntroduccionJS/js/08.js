const cliente = 'Juan';
const saldo = 400;

console.log(`El cliente ${cliente} tiene un saldo de ${saldo}€ en su cuenta`);

const producto = {
    nombre: 'Tablet',
    precioProducto: 300,
    disponible: true
}